<% if logged_in? %>
	<div class="row">
			<div class="container">
			<aside class="col-md-4">
				<section class="user_info">
					<%= render 'shared/user_info' %>
				</section>
				<section class="nazo_form">
					<%= render 'shared/nazo_form' %>
				</section>
			</aside>
		</div>
	</div>

<%end %>

