
<% provide(:title,@user.name) %>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
				<%= gravatar_for @user,{size:90} %>
				おなまえ:<%= @user.name %><br>

				now:<%= Time.now %><br>
			</h1>
		</section>
	</aside>
</div>

